tr=c(T,TRUE)
fa=c(F,FALSE)
f=c(1,0,T)
# язык "Р" воспринимает информацию в виде вектора (т.е. 1,2,3- три числа для него выглядят как вектор). Информацию в нем можно вводить в виде текстовой инфы("х","у"), числовой (1,2,3) и логической (TRUE,FALSE,T,F)
plot(density(rnorm(1:100)),col="yellow")
